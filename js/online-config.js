/* Padrão: sinalização gratuita PeerServer Cloud + STUN do PeerJS.
   Para uma implantação com seu próprio servidor, preencha host/port/path/secure.
   TURN opcional entra em config.iceServers. Nunca publique segredos permanentes:
   use credenciais TURN temporárias obtidas de um backend quando necessário. */
KT.ONLINE_CONFIG = {};
