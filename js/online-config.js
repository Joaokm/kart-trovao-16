/* relayUrl: endereço do servidor de salas (servidor/, publicado com `npm run deploy`), no formato
   'wss://kart-trovao-salas.SUBDOMINIO.workers.dev'. Em localhost dá para apontar outro com ?relay=ws://127.0.0.1:8787. */
KT.ONLINE_CONFIG = {
  relayUrl: ''
};
